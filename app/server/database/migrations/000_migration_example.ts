import type { Migration } from "../../types/migrate.js";

export const migration: Migration = {
    name: "",

    async up(query) {
        await query(`

        `);
    },

    async down(query) {
        await query(`

        `);
    }
};